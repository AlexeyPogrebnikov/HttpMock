<Window x:Class="TcpMock.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:client="clr-namespace:TcpMock.Client"
        Title="TcpMock" Height="450" Width="800">
  <Window.DataContext>
    <client:MainWindowViewModel />
  </Window.DataContext>
  <Grid>
    <StackPanel>
      <ToolBar>
        <Button Content="New..." />
      </ToolBar>
      <StackPanel Orientation="Horizontal">
        <Label Content="Host:" />
        <TextBox Text="{Binding ConnectionSettings.Host}" Width="200" />
        <Label Content="Port:" />
        <TextBox Text="{Binding ConnectionSettings.Port}" Width="50" />
        <Button Content="Start" Command="{Binding StartTcpServer}" CommandParameter="{Binding ConnectionSettings}"
                Visibility="{Binding StartTcpServerVisibility}" />
        <Button Content="Stop" Command="{Binding StopTcpServer}" Visibility="{Binding StopTcpServerVisibility}" />
      </StackPanel>
      <TabControl>
        <TabItem Header="Mocks">
          <ListView ItemsSource="{Binding MockListViewItems}">
            <ListView.View>
              <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding Caption}" />
              </GridView>
            </ListView.View>
          </ListView>
        </TabItem>
        <TabItem Header="{Binding RequestsHeader}">
          <ListView ItemsSource="{Binding RequestListViewItems}">
            <ListView.View>
              <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding Time}" />
                <GridViewColumn DisplayMemberBinding="{Binding Url}" />
              </GridView>
            </ListView.View>
          </ListView>
        </TabItem>
      </TabControl>
    </StackPanel>
  </Grid>
</Window>